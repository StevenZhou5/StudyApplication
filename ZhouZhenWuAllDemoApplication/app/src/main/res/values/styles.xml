<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <declare-styleable name="GifView">
        <attr name="gif" format="reference" />
        <attr name="paused" format="boolean" />
    </declare-styleable>
    <declare-styleable name="CustomTheme">
        <attr name="gifViewStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="PointPageIndicator">
        <attr name="pointDistance" format="float"/>
        <attr name="backPointColor" format="color" />
        <attr name="backPointRadius" format="float" />
        <attr name="currentPointColor" format="color" />
        <attr name="currentPointRadius" format="float" />
    </declare-styleable>

    <!--百分比展示控件-->
    <declare-styleable name="PercentTextView">
        <attr name="back_paint_color" format="color" />
        <attr name="back_paint_alpha" format="integer" />
        <attr name="percent_paint_color" format="color" />
        <attr name="percent_paint_alpha" format="integer" />
        <attr name="paint_stoke_width" format="dimension" />
    </declare-styleable>
</resources>
